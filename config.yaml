# ============================================================
# Search Log ETL Configuration
# ============================================================
search:
  base_path: '/Users/alex/BigData-2025/customer-behavior-etl/data/log_search/log_search'
  output_path: '/Users/alex/BigData-2025/customer-behavior-etl/output/search_interactions'
  max_keywords: 1000
  batch_size: 20
  write_output: true

# ============================================================
# Content Interactions ETL Configuration (Daily JSON Logs)
# ============================================================
content_interactions:
  base_path: '/Users/alex/BigData-2025/customer-behavior-etl/data/log_content'
  output_path: '/Users/alex/BigData-2025/customer-behavior-etl/output/content_interactions'
  start_date: '20220401' # Format: YYYYMMDD
  end_date: '20220420' # Format: YYYYMMDD
  write_output: true
  categories:
    - 'Giải Trí' # Entertainment (RELAX)
    - 'Phim Truyện' # Movies/Series (VOD, FIMS, BHD, DANET)
    - 'Thiếu Nhi' # Children (CHILD)
    - 'Thể Thao' # Sports (SPORT)
    - 'Truyền Hình' # TV/Broadcast (CHANNEL, DSHD, KPLUS)

# ============================================================
# Spark Configuration (shared)
# ============================================================
spark:
  app_name: 'CustomerBehaviorETL'
  master: 'local[*]' # Change to cluster config for production
  log_level: 'WARN'
